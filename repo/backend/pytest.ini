[pytest]
pythonpath = .
testpaths = unit_tests API_tests integration
markers =
    concurrent: tests that exercise real concurrent behavior against a database
    postgres: tests that require a PostgreSQL database
    integration: external HTTP tests that hit the live Docker service over the network
